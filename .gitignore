# Rust
/target/

# OpenCode runtime (keep tools/, plugins/, package.json tracked)
.opencode/node_modules/

# ctx_plugin (bun install output; lockfile stays tracked)
ctx_plugin/node_modules/

# Superpowers / local experiments
.superpowers/
