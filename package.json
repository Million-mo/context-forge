{
  "name": "context-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Context management for AI coding agents",
  "workspaces": [
    "mcps/*"
  ],
  "overrides": {
    "zod": "^3.25.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  },
  "scripts": {
    "install:all": "tsx scripts/install-all.ts",
    "install:all:uninstall": "tsx scripts/install-all.ts --uninstall",
    "build:shared-types": "npm run build --workspace=@context-forge/shared-types",
    "build:ctx-forge": "npm run build:shared-types && npm run build --workspace=@context-forge/mcp-context-forge",
    "build:all": "npm run build:ctx-forge",
    "typecheck": "npm run typecheck --workspace=@context-forge/shared-types && npm run typecheck --workspace=@context-forge/mcp-context-forge"
  }
}
